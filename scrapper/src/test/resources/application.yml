app:
  scheduler:
    enable: true
    interval: 7s
    force-check-delay: 10s
  url_client:
    github_default_url: http://localhost:8080
    stackoverflow_default_url: http://localhost:8080

spring:
  application:
    name: scrapper

server:
  port: 8080

logging:
  config: classpath:log4j2-plain.xml
